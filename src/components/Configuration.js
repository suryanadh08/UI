import React from 'react';
import { Box, Typography } from '@mui/material';

const Configuration = () => {
  return (
    <Box sx={{ marginTop: 4 }}>
      <Typography variant="h6">Configuration</Typography>
      <Typography variant="body1">This page allows you to configure the rule engine settings.</Typography>
    </Box>
  );
};

export default Configuration;
